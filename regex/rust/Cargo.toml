[package]
name = "rust"
version = "0.1.0"
edition = "2024"
license-file = "../../LICENSE"

[lib]
path = "lib.rs"
doctest = false

[dependencies]
num-traits = "0.2"
regex = "1"
regex-automata = "0.4"
regex-syntax = "0.8"

[dev-dependencies]
googletest = "0.14"
