# Stemming

Wrapper for the [snowball](https://github.com/snowballstem/snowball) library.

## Usage

Requires installation of clang and a nightly version of Rust.

```
cd stemming
cmake -B build
cmake --build build --parallel
```
