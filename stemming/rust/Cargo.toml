[package]
name = "rust"
version = "0.1.0"
edition = "2021"
license-file = "../../LICENSE"

[lib]
path = "../libstemmer.rs"
crate-type = ["staticlib"]
doctest = false

[dependencies]
isolang = "2.0"
snowball_stemmer = { package = "rust-stemmers", version = "1.2" }
