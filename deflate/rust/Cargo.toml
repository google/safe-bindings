[package]
name = "rust"
version = "0.1.0"
edition = "2024"
license-file = "../../LICENSE"

[lib]
path = "lib.rs"
doctest = false

[dependencies]
bytes = "1"
flate2 = { version = "1", features = ["rust_backend", "zlib-rs"] }
